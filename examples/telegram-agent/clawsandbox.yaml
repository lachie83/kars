# Kars Example: Telegram-Connected Agent
#
# Deploys an OpenClaw agent with Telegram channel integration.
# The agent responds to Telegram messages and optionally uses Bing web search
# (auto-discovered via Foundry /connections API when Bing Grounding is connected).
#
# Channel pattern:
#   CLI flag (--telegram-token) → env var (TELEGRAM_BOT_TOKEN)
#     → entrypoint.sh auto-config → plugins.allow + plugins.entries + channels.telegram
#
# Credentials are stored in a K8s secret named "telegram-agent-credentials"
# and mounted via envFrom (optional: true). Create with:
#   kars credentials update telegram-agent --telegram-token "YOUR_TOKEN"
#
# Or deploy via CLI (creates the secret automatically):
#   kars add telegram-agent --channels telegram --telegram-token "YOUR_TOKEN"

---
apiVersion: kars.azure.com/v1alpha1
kind: InferencePolicy
metadata:
  name: telegram-agent-inference
  namespace: kars-system
  labels:
    kars.azure.com/sandbox: telegram-agent
spec:
  appliesTo:
    sandboxName: telegram-agent
  modelPreference:
    primary:
      provider: azure-openai
      deployment: gpt-4.1
  contentSafety:
    requirePromptShields: true
  tokenBudget:
    dailyTokens: 500000
    perRequestTokens: 128000
---
apiVersion: kars.azure.com/v1alpha1
kind: KarsSandbox
metadata:
  name: telegram-agent
  namespace: kars-system
  labels:
    kars.azure.com/channels: telegram
spec:
  runtime:
    kind: OpenClaw
    openclaw:
      # Image and version intentionally unset: the controller resolves
      # the sandbox image from its SANDBOX_IMAGE env (set by `kars
      # up`). Pinning a registry path here breaks the example for users
      # who haven't pushed to the same registry.
      config:
        agent:
          model: "azure/gpt-4.1"

  sandbox:
    isolation: "enhanced"
    seccompProfile: "kars-strict"
    readOnlyRootFilesystem: true
    runAsNonRoot: true
    allowPrivilegeEscalation: false
    writablePaths:
      - /sandbox
      - /tmp

  inferenceRef:
    name: telegram-agent-inference

  # Egress rules — Telegram Bot API + optional Bing search
  networkPolicy:
    defaultDeny: true
    allowedEndpoints:
      # Telegram Bot API (required for channel)
      - host: "api.telegram.org"
        port: 443
      # GitHub (common for agent tools)
      - host: "github.com"
        port: 443
      - host: "api.github.com"
        port: 443

  azureServices:
    - service: ai-foundry
      permissions: [inference, evaluate]

  resources:
    requests:
      cpu: "500m"
      memory: "1Gi"
    limits:
      cpu: "2"
      memory: "4Gi"
