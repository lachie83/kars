{{- if .Values.monitoring.inspektorGadget.enabled }}
# Inspektor Gadget is deployed via 'kubectl gadget deploy' (official method).
# This is a placeholder — the actual DaemonSet is managed by the kubectl-gadget plugin.
#
# To install:
#   kubectl krew install gadget
#   kubectl gadget deploy
#
# Usage:
#   kubectl gadget run trace_open:latest -n azureclaw-<sandbox-name>
#   kubectl gadget run trace_exec:latest -n azureclaw-<sandbox-name>
#   kubectl gadget run trace_tcp:latest -n azureclaw-<sandbox-name>
#   kubectl gadget run trace_dns:latest -n azureclaw-<sandbox-name>
#
# Or via AzureClaw CLI:
#   azureclaw trace <sandbox-name> --exec
#   azureclaw trace <sandbox-name> --network
#   azureclaw trace <sandbox-name> --dns
{{- end }}
