apiVersion: monitoring.coreos.com/v1
kind: PodMonitor
metadata:
  name: kars-sandbox-router
  namespace: monitoring
  labels:
    app.kubernetes.io/part-of: kars
    release: kps
spec:
  # Scrape every kars sandbox pod across all kars-* namespaces.
  # The router /metrics is on port 8443 (HTTP, cluster-internal — the
  # TLS-terminating ingress is loopback-only).
  namespaceSelector:
    any: true
  selector:
    matchLabels:
      kars.azure.com/component: sandbox
  podMetricsEndpoints:
    - port: inference        # container port name on the router container
      path: /metrics
      interval: 15s
      scrapeTimeout: 10s
      scheme: http
      relabelings:
        - sourceLabels: [__meta_kubernetes_pod_label_kars_azure_com_sandbox]
          targetLabel: sandbox
        - sourceLabels: [__meta_kubernetes_namespace]
          targetLabel: sandbox_namespace
