# Demo scenario step 1 — minimal governed sandbox.
#
# Applies an InferencePolicy + KarsSandbox, expects router echo Ready.
# Shared by tools/demo/run-demo.sh across dev / kind / aks modes.
---
apiVersion: kars.azure.com/v1alpha1
kind: InferencePolicy
metadata:
  name: demo-inference
  namespace: kars-system
  labels:
    kars.azure.com/sandbox: demo-agent
spec:
  appliesTo:
    sandboxName: demo-agent
  modelPreference:
    primary:
      provider: azure-openai
      deployment: gpt-4.1
  contentSafety:
    requirePromptShields: true
  tokenBudget:
    perRequestTokens: 32000
---
apiVersion: kars.azure.com/v1alpha1
kind: KarsSandbox
metadata:
  name: demo-agent
  namespace: kars-system
spec:
  runtime:
    kind: OpenClaw
    openclaw:
      version: "2026.3.13"
      image: karsacr.azurecr.io/openclaw-sandbox:latest
  sandbox:
    isolation: enhanced
    seccompProfile: kars-strict
    readOnlyRootFilesystem: true
    runAsNonRoot: true
    allowPrivilegeEscalation: false
    writablePaths: [/sandbox, /tmp]
  inferenceRef:
    name: demo-inference
  networkPolicy:
    defaultDeny: true
    allowedEndpoints:
      - host: github.com
        port: 443
  azureServices:
    - service: ai-foundry
      permissions: [inference]
  resources:
    requests: { cpu: "500m", memory: "1Gi" }
    limits:   { cpu: "2",    memory: "4Gi" }
