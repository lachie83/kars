You orchestrate three sub-agents to produce a 2-page executive brief on the state of agentic AI runtime environments in 2026. Your role is orchestration only — do not call foundry tools yourself except for the single DeepWiki MCP query in Step 1a.

Step 1. Call kars_discover('*') as your first action.

Step 1a. Call the DeepWiki MCP tool via the execbrief-deepwiki MCP server: invoke `ask_question` (or the equivalent MCP tool exposed by execbrief-deepwiki) with question="How does kars enforce isolation, egress, attestation, and governance for sandboxed agents? Cite the relevant docs." and repository="microsoft/agent-governance-toolkit". Capture the answer into a short variable you can paste into the analyst dispatch. This step is mandatory — the run is verified to require ≥1 real MCP tools/call on the parent router.

Step 2. For each of analyst, viz, writer that is missing, call kars_spawn with role text ('research analyst', 'data visualisation engineer', 'technical writer') and governance=true.

Step 3. Dispatch task briefs to each sub-agent with kars_mesh_send (back to back, do not await between dispatches):

to_agent='analyst' message=
"Research brief: state of agentic AI runtime environments in 2026. Use foundry_web_search with ≥3 distinct queries to gather ≥6 https URLs covering leading agentic AI runtimes from major cloud and AI providers. Build a JSON artifact with: trends (5–8), control_categories (3 named with citations), runtimes (≥4 × 4 cols: workload_boundary, network_posture, attestation_story, governance_integration), metrics (4-runtime 0–10 scorecard across those 4 cols). Cite each claim with URL + date.

Grounding context from the parent's DeepWiki query about kars's own posture (use to calibrate the 'control_categories' framing, not as a citation):
<<DEEPWIKI_ANSWER>>

CRITICAL — file persistence: foundry_code_execute runs in an EPHEMERAL Foundry container — files at /mnt/data/* are NOT visible to your sandbox FS and CANNOT be mesh-transferred. To produce analyst.json:
  1. Build the JSON object incrementally in your reasoning (do NOT try to write 8 KB inside a single tool argument — that blows the JSON-arg parse limit).
  2. Use ONE file_write call to path /sandbox/.openclaw/workspace/analyst.json with the COMPLETE minified JSON (no indentation, no newlines inside the JSON). Aim for ≤4 KB total: keep field names short, drop redundant prose, but keep every URL verbatim.
  3. If that single file_write fails JSON-arg parse: trim trends to 5, drop any 'notes' fields, and retry once minified harder.
  4. Then kars_mesh_transfer_file twice: once to to_agent='viz' (file_path=/sandbox/.openclaw/workspace/analyst.json), once to to_agent='writer' (same file_path).

Do NOT use foundry_code_execute for writing analyst.json — that path is unreliable across container boundaries. Do NOT attempt shell/bash/python execution outside foundry_code_execute — local shell is policy-blocked.

Sparse first-query results are normal; broaden queries. Do NOT attempt any MCP tools — they are not available in your sandbox; web_search is sufficient. Do not report blocked unless ≥3 query variants AND your inbox AND kars_discover('*') all turn up nothing."

NOTE: Before sending the analyst dispatch, substitute `<<DEEPWIKI_ANSWER>>` with the actual answer text you received in Step 1a (trim to ≤800 chars if needed). If Step 1a failed, replace the placeholder with the literal string 'Step 1a DeepWiki query failed; analyst should rely on web_search only.' and proceed.

to_agent='viz' message=
"You will receive analyst.json from analyst over the mesh. First call kars_mesh_await(senders=['analyst'], timeout_seconds=600). When the file arrives:

CRITICAL: foundry_code_execute runs in a Foundry-managed container; it CANNOT see /sandbox/.openclaw/workspace/incoming/. So read the analyst JSON LOCALLY first via file_read, then EXTRACT only the small `metrics` sub-object and embed THAT (NOT the full analyst.json) inside the code argument to foundry_code_execute. Embedding the entire analyst.json (multiple KB) corrupts the tool-call envelope and triggers JSON-parse failures — embed only `data["metrics"]` (≤1 KB). foundry_image_generation, by contrast, persists the PNG directly into your sandbox FS at /sandbox/.openclaw/workspace/<output_filename> — use the output_filename parameter to pick a stable name.

MANDATORY tool sequence:
(1) file_read /sandbox/.openclaw/workspace/incoming/analyst.json — parse the result yourself and isolate ONLY the `metrics` field (a small dict, typically <1 KB). Discard `trends`, `control_categories`, `runtimes`, and any other fields BEFORE you build the code argument; writer needs those, you do not.
(2) foundry_code_execute (python, matplotlib) — pass a code argument that begins with `metrics = json.loads(r'''<paste ONLY the metrics sub-object verbatim, minified>''')` then render a 4-runtime grouped bar chart with x-axis tick labels for the four columns (workload_boundary, network_posture, attestation_story, governance_integration) and y-axis 0–10. Use matplotlib savefig to /mnt/data/scorecard.png at 1024x640. The runtime wrapper auto-downloads /mnt/data outputs to /sandbox/.openclaw/workspace/scorecard.png — that is the path you mesh_transfer. If the tool-call args STILL fail JSON parse, do NOT escalate to blocked: retry with the metrics dict shrunken further (e.g. round 1-decimal scores), keep arguments small.
(3) foundry_image_generation model='gpt-image-1' size='1024x1024' output_filename='hero.png' for a hero image titled 'Agentic AI Runtimes 2026 — Defense in Depth' (isometric data-center cutaway, Azure cyan accents). The PNG will land at /sandbox/.openclaw/workspace/hero.png — do NOT pass /mnt/data anywhere for this tool.
(4) kars_mesh_transfer_file three times to to_agent='writer': /sandbox/.openclaw/workspace/scorecard.png, /sandbox/.openclaw/workspace/hero.png, and /sandbox/.openclaw/workspace/incoming/analyst.json (re-forwarded).

DO NOT fall back to local matplotlib or SVG: this run is verified by checking that foundry_code_execute and foundry_image_generation appear in your router log AND that the mesh-transfer of both PNGs is acknowledged by writer."

to_agent='writer' message=
"You will receive analyst.json and two PNGs from analyst+viz. First call kars_mesh_await(senders=['analyst','viz'], timeout_seconds=900). Then produce a 2-page brief on the state of agentic AI runtime environments in 2026.

CRITICAL: Earlier attempts at chunked file_write with append=true failed because the tool OVERWRITES each call. You MUST emit the complete brief in ONE file_write call. To keep the tool argument under JSON parse limits, target ~700-800 words and keep the markdown simple: no triple backticks, no nested code fences, no JSON blobs in the body, plain ASCII punctuation, no backslash escapes other than '\\n'. ~5 KB of content fits comfortably.

The brief MUST include (in this order):
- hero markdown at top: ![hero](/sandbox/.openclaw/workspace/incoming/hero.png)
- ≤80-word hook
- 3 numbered sections (Trends / Control Domains / Runtime Landscape ~180-200 words each)
- chart markdown in Runtime Landscape: ![scorecard](/sandbox/.openclaw/workspace/incoming/scorecard.png)
- closing 'So what for kars?' (~130 words)
- numbered footnotes with each analyst citation as a verbatim https URL (no placeholders). Include one footnote citing https://github.com/microsoft/agent-governance-toolkit and explicitly use the word DeepWiki in that footnote line so the governance posture is visible.
- The four control-domain terms MUST appear verbatim in the body at least once each: `isolation`, `egress`, `attestation`, `governance` — the Runtime Landscape section is the natural place when describing the scorecard columns.
- The hero and scorecard PNGs are real attachments at /sandbox/.openclaw/workspace/incoming/{hero.png,scorecard.png} — assume they are present and reference them; do NOT add caveats like 'missing image' to the body.

Procedure:
1. kars_mesh_await(senders=['analyst','viz'], timeout_seconds=900) — wait for both deliveries.
2. file_read /sandbox/.openclaw/workspace/incoming/analyst.json — read the citations and metrics.
3. ONE file_write call: path=/sandbox/.openclaw/workspace/brief.md, content=<full brief markdown, ~700-800 words>. No append flag, no chunking.
4. kars_mesh_transfer_file(to_agent='parent', file_path='/sandbox/.openclaw/workspace/brief.md', description='final brief').
5. kars_mesh_send(to_agent='parent', message='final brief delivered').

If file_write returns a JSON parse error on its arguments, trim the body by ~20% (shorten the longer-form sections; keep every citation) and retry once. If two consecutive file_write attempts both fail, surface the exact error to parent and stop."

Step 4. Call kars_mesh_await(senders=['writer'], timeout_seconds=900). Sub-agents exchange artifacts directly over the mesh; you are not in the data path.

Step 5. Read writer's delivered file via file_read('/sandbox/.openclaw/workspace/incoming/brief.md'). Your FINAL ASSISTANT REPLY MUST BE the EXACT, CHARACTER-FOR-CHARACTER contents of that file — nothing before it, nothing after it, no summary, no commentary, no quote fences. The reply must START with the first line of brief.md (which will be `![hero](/sandbox/.openclaw/workspace/incoming/hero.png)`) and END with the last footnote line. DO NOT shorten or paraphrase the brief; downstream verification literally diffs your response against the file. If the file is empty or much shorter than 500 words, surface a block status instead.

If a sub-agent replies with status=blocked, re-dispatch its brief once with explicit guidance to address the named cause (e.g. for `tool_failure` on foundry_code_execute with a JSON-parse hint, instruct the sibling to shrink the embedded payload to the minimum subset needed). After the re-dispatch, call kars_mesh_await(senders=['<that sibling>'], timeout_seconds=900) and wait for either a fresh artifact OR a second blocked status before finalizing — do NOT synthesize the brief while the retry is still in flight. If it blocks twice, surface the reason and stop — do not substitute your own work. If kars_mesh_await times out, run kars_discover and kars_spawn_status to diagnose, then surface status and stop.

Go.
